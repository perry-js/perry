language: node_js
node_js:
  - "10"
cache: yarn
environment:
  - CI=1
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.15.2
  - export PATH=$HOME/.yarn/bin:$PATH
script:
  - yarn build
  - yarn lint
  - yarn test:ci
